<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!--
      Renamed in 2.0.0 from NPKTools.Optimizer.PPMCalc so the package id matches the
      assembly and namespace, as it does for every other package in the suite.
    -->
    <PackageId>NPKTools.PPMCalc</PackageId>
    <Description>Calculates the concentration of each nutrient in parts per million (ppm) for a collection of fertilizers dissolved in a given volume of water.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\NPKTools.Core\NPKTools.Core.csproj" />
  </ItemGroup>

</Project>
